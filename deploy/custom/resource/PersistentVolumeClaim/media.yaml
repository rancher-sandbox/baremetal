---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: media
  namespace: ilo
spec:
  storageClassName: local
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 5Mi
